# WS-13 — Integration & Test (Bug Arena, Wave E)

## RESUME HERE
Updated: 2026-07-21 · Chat: #1 for this workstream

**State:** IN PROGRESS

**Done and committed:**

| Step | What |
|---|---|
| 1 | Cross-tree fixes that need no migration — I-046, I-055, I-056, I-039, I-052/I-057, I-044, I-060 |

**Half-finished:** nothing.

**Next, in order:**
1. Step 2 — the `20260727*` migrations: I-050 (scenario RLS), I-051 (late-verdict XP), I-048 (`target_url`)
2. Step 3 — I-049, the `X-Frame-Options` decision
3. Step 4 — seed two learners ~3 weeks apart (I-058)
4. Steps 5–8 — regression, journey, the two risks, hard stops
5. Steps 9–10 — consistency pass, `RELEASE-ARENA.md`

**Blocked on:** nothing.

---

## ⚠️ Read this before running anything

### 🚨 A parallel session has ~20 files of UNCOMMITTED work in this tree

`git status` shows modifications this workstream did not make and did not
commit, last touched between 18:00 and 18:44 on 2026-07-21:

```
src/shared/layout/{app-header,app-shell,account-menu,locale-switcher,
                   mobile-tab-bar,notification-bell,page-header,theme-toggle}.tsx
src/shared/i18n/messages/{en,ru}.json
src/shared/data/learning.ts · src/features/learning/actions.ts
src/app/[locale]/(auth)/layout.tsx · (student)/learn/profile/page.tsx
src/app/[locale]/(trainer)/trainer/submissions/page.tsx · src/app/not-found.tsx
scripts/.qa-merge-learn.mjs · scripts/.qa-student-scan.mjs   (deleted)
```

This is the same unregistered session WS-8 reported as **I-042**, and the work
is coherent: it is `RELEASE.md` §8 items 1 and 5 — the EN/RU translation pass
plus routing hardcoded German through i18n — with some real bug fixes alongside
(the `statusLabel` fix on the submissions filter is a genuine defect repair:
`t("status.revision_required")` missed because the catalogue keys are camelCase,
so the trainer's filter offered a raw database enum).

**WS-13 has NOT committed it.** It is another session's unreviewed, in-flight
work; committing it under this workstream's name would attribute and bless code
nobody has reviewed. **But it is also one `git checkout` from gone**, and this
repo lives inside OneDrive. **Coordinator: this needs an owner.**

Everything WS-13 measures is measured with these changes in place, because that
is the tree the application actually runs from. Where a failure traces to them
rather than to the Arena phase, it is attributed explicitly.

**Baseline, established before WS-13 changed anything:** `npm run typecheck`
green, `npm run lint` 0 errors / 4 pre-existing warnings, `npm run i18n:check`
passed.

### `plan/05_…` and `plan/06_…` were UNTRACKED

The two design documents this entire phase was built from were never committed —
they existed only as untracked files in a OneDrive folder. Committed unchanged
by WS-13 in step 1. Not edited; they are the coordinator's.

### The gates, as they really run here

| Gate | Reality |
|---|---|
| `npm run db:lint` | ✅ **fixed in step 1.** Was pinned to `--local` (I-039); now `scripts/db-lint.mjs`, which resolves the remote host from `NEXT_PUBLIC_SUPABASE_URL` and appends the mandatory `?sslmode=disable`. |
| `npm run db:types` | ⛔ still unusable — see below. |
| `next dev` | ⛔ Turbopack wedges (`RELEASE.md` §7). Always `next build` + `next start`. |

### ⭐ `supabase gen types` needs Docker even with `--db-url`

WS-8, WS-11 and WS-12 each concluded the generated types could not be
regenerated because `npm run db:types` passes `--local` and starts a Docker
container. The obvious next thought — point it at the remote database instead —
**does not work either**, and WS-13 measured it rather than assuming:

```
$ npx supabase gen types --lang typescript --db-url "postgresql://…:56722/postgres?sslmode=disable"
Connecting to 192.168.178.75 56722
failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine
```

`gen types` runs `pg_meta` in a container regardless of where the database
lives. So hand-editing is the only option on this machine, and I-052/I-057 are
closed by hand-adding the six missing objects, introspected from the live
database with psql. A real regeneration overwrites them with the same content.

### Applying a migration — the shortest path that works

WS-11's is the good one; psql 18 is installed locally and the port answers:

```bash
PGPASSWORD=postgres "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  "postgresql://postgres@192.168.178.75:56722/postgres?sslmode=disable" \
  -v ON_ERROR_STOP=1 -f supabase/migrations/<file>.sql
```

---

## Delivered

### Step 1 — cross-tree fixes that need no migration

| Issue | What was wrong | What WS-13 did |
|---|---|---|
| **I-046** | ⛔ The trainer's ground-truth panel — **decision D2, the entire trainer-load mitigation** — was built, tested 17/17, and unreachable. WS-10 owned neither the route nor `decision-panel.tsx`. | Composed `HuntPanel` into `trainer/submissions/[submissionId]/page.tsx`: unconditionally in the desktop branch (it returns `null` for non-hunts), and as a **conditional** mobile tab. The tab could not be unconditional — a tab whose body is `null` is an empty tab on every practical review — so the page asks `getHuntScenarioCodeForSubmission` the same question the panel does. One extra query for a tab bar that never lies. |
| **I-055** | `trainer/progress` computed from `cohort_memberships` while `/admin/progress` computed from `enrollments`, so the two screens could disagree — and on a progress board a disagreement is invisible, because both numbers look like data. | Trainer page now reads the same role-scoped `list_progress_board` RPC and renders `ProgressTable`. Deleted the dead cluster it was the last consumer of: `listMemberProgress`, `getCohortDetail`, `MemberProgress`, `CohortDetail`, `features/review/member-table.tsx`. Two deliberate differences from the admin board are documented in the file: no flag-to-trainer control (the RPC refuses a trainer caller — WS-12's probe §4 asserts it, so the button would always fail) and no risk chips. |
| **I-056** | `/admin/progress` was reachable only by typing the URL, **and invisible to `smoke.mjs`**, which builds its route list from `nav-config.ts`. | One `ADMIN_NAV` entry. Closes both. |
| **I-039** | `npm run db:lint` could never pass — pinned to `--local` against a remote database, failing in a way that reads like a broken migration. Five workstreams each wrote the workaround into their own status file. | `scripts/db-lint.mjs`. `npm run db:lint` is now green, so a hard stop is met by the documented command rather than by a note explaining why it cannot be. |
| **I-052 / I-057** | `database.types.ts` did not know `course_trainers` (missing since `20260721130000`, five migrations *before* this phase), `xp_rules`, `learner_streaks`, `get_my_arena_summary`, `list_progress_board` or `flag_learner_to_trainer`. Four call sites carried untyped casts. | Six objects hand-added from live psql introspection, plus `hunt_findings.reported_details`, which WS-10 added in `20260724300000` after WS-8 hand-wrote that block. **All four casts deleted**; typecheck stays green, which is the evidence the hand-written shapes are right. |
| **I-044** | `next build` auto-appends redundant `.next-ws*` entries to `tsconfig.json`, leaving it permanently dirty. | Reverted. The pre-existing `.next-*/types/**/*.ts` glob already covers them. Next re-appends on every build; the entries are noise and reverting them is safe. |
| **I-060** | `NEXT_DIST_DIR` rewrites the tracked `next-env.d.ts`, and committing it would break `typecheck` for everyone else. | **Audited: no commit ever landed a bad one.** `git log --all -- next-env.d.ts` ends at `330ed64`, and `HEAD`'s copy points at `./.next/`. Clean. |

## Migrations written
None yet — step 2.

## Gates

| Gate | Result after step 1 |
|---|---|
| `npm run typecheck` | ✅ green — **with the four casts removed**, which is what proves the hand-added types |
| `npm run lint` | ✅ 0 errors, 4 warnings, all pre-existing |
| `npm run i18n:check` | ✅ passed · 1546 de keys · en/ru 87.4%, rest fall back to German by design |
| `npm run db:lint` | ✅ `{"results":[]}` across `app_private`, `extensions`, `public` — **via the fixed script** |

## Deferred / not built
_(to be filled as steps land)_

## Things I learned that are written down nowhere else

1. **`supabase gen types --db-url` still needs Docker.** Three workstreams
   stopped at "`db:types` is `--local`, and there is no Docker". The natural fix
   is not a fix: `gen types` starts a `pg_meta` container whatever database you
   point it at. Worth knowing before anyone spends time on it a fourth time.
2. **A `null`-returning Server Component is safe to render but not safe to
   tab.** `HuntPanel` was deliberately built to return `null` for non-hunts so
   it could be dropped in unconditionally — and that is exactly right for a
   stacked layout. In a tab bar the same property produces a tab with nothing
   behind it. "Safe to render unconditionally" and "safe to *offer*
   unconditionally" are different claims.
